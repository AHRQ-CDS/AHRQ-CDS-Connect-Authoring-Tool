define "Population":
  <% if(inclusions && exclusions) {%>"<%= inclusionsName %>" and not "<%= exclusionsName %>" <%}
  else if (inclusions) {%> "<%= inclusionsName %>" <%}
  else {%>not "<%= exclusionsName %>"<%}%>
