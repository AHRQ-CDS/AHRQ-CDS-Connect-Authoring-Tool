define "<%=element_name%>":
  <%conditions.forEach((condition, i) => { if(i > 0) {%>
   or<%}%> Exists(C3F.Confirmed(-%>
<%if(active){-%>C3F.ActiveCondition(<%}-%>[Procedure: "<%=condition.name%>"]))<%if(active){%>)<%}-%>
<%})%>
