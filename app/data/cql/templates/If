define "<%=element_name%>": <%
components.forEach((component, index, array) => {
if (index===0) {
  %>
  if "<%=component.condition.name%>" then 
    "<%=component.block.name%>" <%
} else if (index===array.length-1) {
  %>
  else <%if(component.block.name === 'null'){%>null<%}else{%>"<%=component.block.name%>"<%}%> <%
} else {
  %>
  else if "<%=component.condition.name%>" then
    "<%=component.block.name%>" <%
}
})%>
