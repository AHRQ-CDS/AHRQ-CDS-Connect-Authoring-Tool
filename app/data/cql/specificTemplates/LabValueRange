define "<%-element_name%>":
  <%-observation.map(function(c) { return `"${c.name}"`; })
  %> in <%if (lower_bound_exclusive) {%>(<%} else {%>[<%}%><%=lower_bound%>, <%=upper_bound%><%if (upper_bound_exclusive) {%>)<%} else {%>]<%}%>
