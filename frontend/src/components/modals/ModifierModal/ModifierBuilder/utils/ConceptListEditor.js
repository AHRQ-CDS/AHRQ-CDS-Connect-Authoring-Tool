import React from 'react';
import propTypes from 'prop-types';

const ConceptListEditor = ({}) => {
  return <>hi</>;
};

ConceptListEditor.propTypes = {
  test: propTypes.string
};
export default ConceptListEditor;
