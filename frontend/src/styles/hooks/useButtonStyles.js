import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(
  theme => ({
    iconButton: {
      margin: '-20px 0',
      transform: 'scale(0.8)'
    }
  }),
  { name: 'Buttons' }
);
