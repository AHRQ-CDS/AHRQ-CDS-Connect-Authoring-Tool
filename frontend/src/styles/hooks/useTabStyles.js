import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(
  theme => ({
    tabButtons: {
      display: 'flex',
      justifyContent: 'flex-end'
    }
  }),
  { name: 'Tab', index: 1 }
);
